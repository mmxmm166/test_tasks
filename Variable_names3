7.1
SelectShape - isSelectShape 
flUndo - IsUndo
_ascending - isAscenging
fl_sort - IsSort
asc - IsAc

7.2
bool Rezult = false; // было
bool Found = false;  //стало


 while (!fl_sort) // было
 while (!done) // стало
 
7.3 
//в  целом использую данные принципы везде
//нашел случай  когда использовал l
for (int l=0;l<=1;l++)

//тут лучше использовать
for (int level=0;level<=1;level++)

//пример кода где x и y и h тоже говорят сами за себя, координаты x,y и  высота - h
for (int y = 0; y < h; y++)
		{
			for (int x = 0; x < w; x++ )
7.4
//"Попробуйте найти в своих решениях два-три случая, когда можно использовать пары имён - антонимы."
	не нашел таких примеров

7.5 
sss - StrOfAnalyze
//строка для анализа содержащая исходные данные

s1 - InputStr1
s2 - InputStr2
//введенной через коносль значение переменной с исходными данными


	



